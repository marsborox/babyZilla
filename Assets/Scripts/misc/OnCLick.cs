using UnityEngine;

public class OnClick : MonoBehaviour
{
    private void OnMouseDown()
    {
        if (Input.GetMouseButtonDown(0))
        { 
            //Do this if mouse is clicked
            //prob will need some "interfaceing" method
        }
    }
}
